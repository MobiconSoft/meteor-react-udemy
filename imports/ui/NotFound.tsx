import * as React from 'react';

export interface NotFoundProps {
}

export default class NotFound extends React.Component<NotFoundProps, any> {
  public render() {
    return (
      <div>
        NotFound
      </div>
    );
  }
}
